void setup() {
  Serial.begin(9600); // Opens communication
}

void loop() {
  if (Serial.available() > 0) {
    char cmd = Serial.read();
    Serial.print(cmd); // Echoes the command back to the Pi
  }
}
