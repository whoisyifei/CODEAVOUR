import requests

# When you want to trigger the Build
def on_button_click():
    try:
        # This tells the Pi to run Step 2 (NPM Build)
        requests.get("http://127.0.0.1:5000/run/2")
    except:
        print("Bridge not running!")
